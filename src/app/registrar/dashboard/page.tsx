import Link from "next/link";
import { AppShell, SectionHeading, StatusPill } from "@/components/app-shell";
import { pendingRequests } from "@/lib/mock-data";

export default function RegistrarDashboard() { return <AppShell role="registrar"><header className="topbar"><div><span className="eyebrow">Registrar portal · Tuesday, September 4</span><h1 className="display-font page-title">Good morning, Alex.</h1><p className="muted">Here is today’s document request workload.</p></div><span className="avatar">AD</span></header><div className="grid-3"><div className="surface stat"><span className="muted">Pending review</span><strong className="stat-number">08</strong><span className="muted" style={{ fontSize: 12 }}>Requests needing action</span></div><div className="surface stat"><span className="muted">Ready for release</span><strong className="stat-number">12</strong><span className="muted" style={{ fontSize: 12 }}>Awaiting student pickup</span></div><div className="surface stat"><span className="muted">Completed this month</span><strong className="stat-number">146</strong><span className="muted" style={{ fontSize: 12 }}>Across all document types</span></div></div><SectionHeading title="Requests needing review" link="View all requests" href="/registrar/requests/NU-2026-00421" /><div className="surface table-wrap"><table className="data-table"><thead><tr><th>Request number</th><th>Student</th><th>Document</th><th>Submitted</th><th>Status</th><th></th></tr></thead><tbody>{pendingRequests.map((request) => <tr key={request.id}><td><strong>{request.id}</strong></td><td>{request.student}</td><td>{request.document}</td><td>{request.submitted}</td><td><StatusPill tone="pending">{request.status}</StatusPill></td><td><Link className="link" href={`/registrar/requests/${request.id}`}>Review →</Link></td></tr>)}</tbody></table></div></AppShell>; }